using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

public static class StringUtil
{
    public static string MD5Encrypt(this string text)
    {
        //Local onde será adicionado o código para tranformar uma string em um hash usando md5
        return text;
    }

    public static string Encrypt(this string text)
    {
        //Local onde será adicionado o código para uso de criptografia simétrica
        return text;
    }

    public static string Decrypt(this string text)
    {
        //Local onde será adicionado o código para desfazer a criptografia simétrica
        return text;
    }
}